# 今日规划 Implementation Plan

**Goal:** 交付个人每日时间规划网站及免费发布配置。
**Architecture:** Vite 构建原生模块，Supabase SDK 管理登录和数据库访问。本地模式独立保存，云端 RLS 隔离用户。
**Tech Stack:** HTML、CSS、JavaScript、Vite、Supabase、Node test、Playwright。

## Global Constraints
中文、手机电脑适配、GitHub Pages 相对资源路径、不提交私密凭据。

- [x] 建立 `src/model.js` 时间与统计函数，并以 `tests/model.test.js` 验证非法时间、相邻时间及日期跨月。
- [x] 建立 `index.html`、`src/style.css`、`src/app.js`，提供日期导航、统计、计划表单和本机持久化。
- [x] 建立 `src/cloud.js`、`supabase.sql`，实现 SDK 登录、用户隔离、版本冲突检查、轮询及失败提示。
- [x] 建立 `.github/workflows/deploy.yml`、`README.md`，提供部署与 OAuth 配置步骤。
- [x] 执行 `npm test`、`npm run build`，浏览器验证桌面与手机的增删改查、持久化及导出；记录云端待配置限制。


验证结果：4 项模型测试通过；1440px 与 390px 浏览器完整操作测试通过；构建通过。真实 Supabase 已连通，检查发现 plans 表缺失、GitHub provider 未启用；控制台需要用户登录。GitHub 远程仓库尚未创建，未发布。
